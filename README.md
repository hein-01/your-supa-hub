# YaYou Web App

This is the YaYou web application built with Vite, React, TypeScript, Tailwind CSS, and shadcn-ui.

## Quick start

```sh
# Install dependencies
npm install

# Start the dev server (default: http://localhost:8080)
npm run dev

# Build for production
npm run build

# Preview the production build
npm run preview
```

## Tech stack

- Vite
- TypeScript
- React
- shadcn-ui
- Tailwind CSS

## Deployment

The app is a Vite SPA. You can deploy the `dist/` output to any static hosting provider (e.g., Vercel, Netlify, GitHub Pages).

1. Build the project: `npm run build`
2. Deploy the contents of `dist/`.

If you need a specific deployment target configured, let me know and I can add the setup steps here.
